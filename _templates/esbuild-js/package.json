{
  "name": "@dschulmeis/template-esbuild-js",
  "version": "1.0.0",
  "description": "esbuild + JavaScript | Template",
  "author": "Dennis Schulmeister-Zimolong",
  "type": "module",
  "scripts": {
    "clean": "node bin/clean.js",
    "build": "npm run clean && node bin/esbuild-bundle.js",
    "start": "node bin/esbuild-serve.js"
  },
  "config": {
    "clean_dirs": "static/_bundle"
  },
  "devDependencies": {
    "esbuild": "^0.21.2",
    "shelljs": "^0.8.5"
  }
}